package com.example.srl

import android.os.Bundle
import androidx.appcompat.app.AppCompatActivity

/**
 * ESTA ES UNA CLASE TEMPORAL (STUB)
 * Su único propósito es existir en el paquete 'com.example.srl'
 * para que el archivo 'dashboar.kt' pueda compilar sin el error de referencia.
 * * DEBERÁS REEMPLAZAR ESTE CONTENIDO CON TU CÓDIGO REAL DE FIREBASE LOGIN DESPUÉS.
 */
class dashboard : AppCompatActivity() {

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        // Puedes dejar el cuerpo vacío.
    }
}